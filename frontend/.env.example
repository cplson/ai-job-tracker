# Optional override for the API base URL.
# Default is /api — works in production (nginx) and local dev (Vite proxy in vite.config.ts).
#
# Examples:
#   VITE_API_URL=/api
#   VITE_API_URL=http://localhost:5001/api
